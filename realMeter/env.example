# Smart Meter Configuration

METER_IP=192.168.1.100
METER_PORT=443

#Optional: Custom certificate paths (if not using default realMeter/certs/)\n# 

CERT_PATH=realMeter/certs/.cert.pem\n# 
KEY_PATH=realMeter/certs/.key.pem
